# Dashboard default config (used when no config is saved yet)
# NEXT_PUBLIC_ exposes these to the browser so the dashboard can read them
# Extension reads these from GET /api/feed-import/config when the app is running (otherwise uses built-in fallbacks)
NEXT_PUBLIC_DEFAULT_MAX_POSTS=100
EXTENSION_SCROLL_PAUSE_MS=2500
EXTENSION_NO_NEW_POSTS_EXIT=6
EXTENSION_MAX_POSTS_FALLBACK=200
NEXT_PUBLIC_APP_ORIGIN=http://localhost:3001
NEXT_PUBLIC_DEFAULT_SHORTLIST_SIZE=10
# High-engagement rule (reactions, comments, reposts only; follower count not used)
NEXT_PUBLIC_DEFAULT_MIN_REACTIONS=100
NEXT_PUBLIC_DEFAULT_MIN_COMMENTS=20
NEXT_PUBLIC_DEFAULT_MIN_REPOSTS=10
# Your LinkedIn profile username for "mentions me" rule (e.g. from linkedin.com/in/USERNAME)
NEXT_PUBLIC_MY_LINKEDIN_USERNAME=

# LinkedIn Developer App (for hybrid: Playwright URNs + API fetch)
# From https://www.linkedin.com/developers/ → your app → Auth tab
LINKEDIN_CLIENT_ID=your_client_id
LINKEDIN_CLIENT_SECRET=your_primary_client_secret
# See docs/HOW_TO_GET_LINKEDIN_ACCESS_TOKEN.md for full guide
# LINKEDIN_ACCESS_TOKEN=your_oauth_access_token
# LINKEDIN_REFRESH_TOKEN=

# AI enrichment: Hugging Face Inference (free tier)
# 1. Get your API key: https://huggingface.co/settings/tokens → New token → enable "Read" (or "Make calls to the serverless Inference API")
# 2. Pick a model (suggested free models):
#    Qwen2 0.5B:  https://api-inference.huggingface.co/models/Qwen/Qwen2-0.5B-Instruct
#    Phi-3 mini:   https://api-inference.huggingface.co/models/microsoft/Phi-3-mini-4k-instruct
#    Flan-T5:      https://api-inference.huggingface.co/models/google/flan-t5-large
HUGGINGFACE_INFERENCE_URL=https://api-inference.huggingface.co/models/Qwen/Qwen2-0.5B-Instruct
HUGGINGFACE_API_KEY=

# Capture script: show browser (false) or run headless (true). Single place for dashboard + CLI.
CAPTURE_HEADLESS=false
# Stop after this many consecutive iterations with no new posts (e.g. when you manually scroll then stop). Default 3.
# CAPTURE_NO_NEW_POSTS_EXIT=3

# Optional: local LMStudio (comment out when using Hugging Face above)
# LMSTUDIO_BASE_URL=http://localhost:1234
